% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/video.R
\name{frame_extract_keyframes}
\alias{frame_extract_keyframes}
\title{Extract keyframes (I-frames) from a video}
\usage{
frame_extract_keyframes(
  video_path,
  output_dir = tempdir(),
  format = "jpg",
  prefix = "keyframe"
)
}
\arguments{
\item{video_path}{Path to video file (single file or vector of paths).}

\item{output_dir}{Directory to save extracted keyframes. Default \code{tempdir()}.}

\item{format}{Output image format. Default \code{"jpg"}.}

\item{prefix}{Filename prefix for extracted frames. Default \code{"keyframe"}.}
}
\value{
A \code{tl_frames} tibble of keyframes with \code{video_source} column.
}
\description{
Extract the encoder's intra-coded frames (I-frames / keyframes) directly
from the video file. Keyframes are full-quality stills embedded by the
encoder; their positions only loosely correlate with semantic scene
changes, so this is a fast, lightweight alternative to shot detection when
you want high-quality representative stills without running the full
shot-boundary pipeline.
}
\details{
\subsection{Keyframes vs shot frames (ELI5)}{
\itemize{
\item \code{frame_extract_shots()} runs tidylens's own shot detection and picks one
representative frame per detected shot. Slow, semantic.
\item \code{frame_extract_keyframes()} reads the encoder's pre-existing I-frames
straight from the file. Fast, no shot detection, but the positions are
decided by the codec (typically every 1-2 seconds), not by content.
}

Uses \code{\link[av:encoding]{av::av_encode_video()}} with the ffmpeg \code{select=eq(pict_type,I)} filter
to pull only I-frames from the video stream. No shot detection is
performed. If the \code{av} path cannot write an image sequence, falls back to
\code{system2("ffmpeg", ...)}.
}
}
\examples{
\dontrun{
keyframes <- frame_extract_keyframes("film.mp4")
}
}
\references{
ffmpeg select filter: \url{https://ffmpeg.org/ffmpeg-filters.html#select}
}
\seealso{
Other video: 
\code{\link{detect_shot_changes}()},
\code{\link{frame_extract_by_seconds}()},
\code{\link{frame_extract_evenly}()},
\code{\link{frame_extract_shots}()},
\code{\link{video_download}()},
\code{\link{video_get_info}()}
}
\concept{video}
