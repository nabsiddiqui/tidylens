% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/texture.R
\name{frame_extract_glcm}
\alias{frame_extract_glcm}
\title{Extract GLCM (Haralick) texture features}
\usage{
frame_extract_glcm(tl_frames, levels = 32, dx = 1, dy = 0, downsample = 200)
}
\arguments{
\item{tl_frames}{A tl_frames tibble.}

\item{levels}{Number of gray levels to quantise to (co-occurrence matrix is
\verb{levels x levels}). Default 32.}

\item{dx}{Horizontal offset of the co-occurrence pair. Default 1.}

\item{dy}{Vertical offset of the co-occurrence pair. Default 0.}

\item{downsample}{Maximum side length for analysis. Default 200.}
}
\value{
The input tibble with added columns:
\itemize{
\item \code{glcm_contrast}: Local variation. 0 for a constant image; higher = busier.
\item \code{glcm_homogeneity}: Inverse of contrast weighted by closeness to the
diagonal. 1 for a constant image; lower = more variation.
\item \code{glcm_energy} (a.k.a. angular second moment): Uniformity of the
co-occurrence distribution. 1 for a constant image; lower = more spread.
\item \code{glcm_entropy}: Shannon entropy of the normalised GLCM. 0 for a constant
image; higher = more disordered texture.
\item \code{glcm_correlation}: Linear dependence of neighbouring gray levels,
in [-1, 1]. Near 1 = smoothly varying; near 0 = uncorrelated or
constant (sd = 0 returns 0 by convention).
}
}
\description{
Compute Gray-Level Co-occurrence Matrix (GLCM) descriptors following
Haralick, Shanmugam & Dinstein (1973). A co-occurrence matrix counts how
often pairs of neighbouring gray levels occur along a given offset, then
summary statistics are derived from it. The five most-used Haralick
features are returned.
}
\details{
\subsection{What it tells you (ELI5)}{

GLCM describes the \emph{texture} of a frame: coarse vs. fine, smooth vs. busy,
directional vs. uniform. A close-up of fabric will score high contrast
and entropy; a flat sky will score near zero on everything.
}
}
\references{
Haralick, R. M., Shanmugam, K., & Dinstein, I. (1973). Textural features
for image classification. \emph{IEEE Transactions on Systems, Man, and
Cybernetics}, SMC-3(6), 610-621.
}
\seealso{
Other texture:
\code{\link{frame_extract_contrast}()},
\code{\link{frame_extract_lbp_histogram}()},
\code{\link{frame_extract_sharpness}()}
}
\concept{texture}
