% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/video.R
\name{video_extract_frames_by_seconds}
\alias{video_extract_frames_by_seconds}
\title{Extract frames from a video}
\usage{
video_extract_frames_by_seconds(
  video_path,
  every = 1,
  output_dir = tempdir(),
  fps = NULL,
  format = "jpg",
  prefix = "frame"
)
}
\arguments{
\item{video_path}{Path to video file (single file or vector of paths).}

\item{every}{Extract one frame every \code{every} seconds. Default 1 (one frame
per second). Internally converted to \code{fps = 1 / every}. Mutually exclusive
with \code{fps}.}

\item{output_dir}{Directory to save extracted frames. Default is \code{tempdir()}.}

\item{fps}{Frames per second to extract. Mutually exclusive with \code{every}.
Provided for advanced use; prefer \code{every} for readability.}

\item{format}{Output image format. Default \code{"jpg"}.}

\item{prefix}{Filename prefix for extracted frames. Default \code{"frame"}.}
}
\value{
A tl_frames tibble of extracted frames with additional column:
\itemize{
\item \code{video_source}: Path to the original video file.
}
}
\description{
Extract frames from a video file at a regular interval. Returns a tl_frames
tibble with a \code{video_source} column tracking the original video.
}
\examples{
\dontrun{
# Extract one frame every 5 seconds
frames <- video_extract_frames_by_seconds("video.mp4", every = 5)

# Extract one frame per second
frames <- video_extract_frames_by_seconds("video.mp4", every = 1)

# Extract from multiple videos
frames <- video_extract_frames_by_seconds(c("video1.mp4", "video2.mp4"), every = 2)
}
}
\seealso{
Other video: 
\code{\link{detect_shot_changes}()},
\code{\link{video_download}()},
\code{\link{video_extract_frames_evenly}()},
\code{\link{video_extract_keyframes}()},
\code{\link{video_extract_shots}()},
\code{\link{video_get_info}()}
}
\concept{video}
