% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/texture.R
\name{frame_extract_lbp_histogram}
\alias{frame_extract_lbp_histogram}
\title{Extract Local Binary Pattern (LBP) histogram}
\usage{
frame_extract_lbp_histogram(
  tl_frames,
  radius = 1,
  n_points = 8,
  bins = NULL,
  downsample = 200
)
}
\arguments{
\item{tl_frames}{A tl_frames tibble.}

\item{radius}{Radius of the LBP neighbourhood. Default 1 (3x3 patch).
Larger radii capture coarser texture but are slower and less stable
on small images.}

\item{n_points}{Number of sampling points around each pixel at the given
radius. Default 8. The uniform-LBP mapping yields
\code{n_points * (n_points - 1) + 3} bins (e.g. 59 for \code{n_points = 8}).}

\item{bins}{Number of histogram bins to return. Default \code{NULL} = the full
uniform-LBP count (\code{n_points * (n_points - 1) + 3}). If fewer bins are
requested, the excess uniform patterns are folded into the final
non-uniform catch-all bin.}

\item{downsample}{Maximum side length for analysis. Default 200.}
}
\value{
The input tibble with added columns:
\itemize{
\item \code{lbp_1}, \code{lbp_2}, ..., \verb{lbp_\{bins\}}: normalised histogram bin counts
(sum to 1 per frame). Frames where the LBP could not be computed
return all-zero bins.
}
}
\description{
Compute a uniform-LBP histogram (Ojala, Pietikainen & Maenpaa, 2002) as a
rotation-invariant micro-texture descriptor. This is a no-CNN \emph{vector
representation}: one fixed-length histogram per frame, comparable across
frames and usable for similarity / clustering / nearest-neighbour search
alongside the existing \code{frame_extract_color_histogram()}.
}
\details{
\subsection{What it tells you (ELI5)}{

A compact fingerprint of local texture patterns: how often each small
3x3 pattern of brighter/darker neighbours occurs. Two frames with similar
micro-texture (e.g. same film stock, same fabric) will have similar LBP
histograms.
}
}
\references{
Ojala, T., Pietikainen, M., & Maenpaa, T. (2002). Multiresolution
gray-scale and rotation invariant texture classification with local
binary patterns. \emph{IEEE Transactions on Pattern Analysis and Machine
Intelligence}, 24(7), 971-987.
}
\seealso{
Other texture:
\code{\link{frame_extract_contrast}()},
\code{\link{frame_extract_glcm}()},
\code{\link{frame_extract_sharpness}()}
}
\concept{texture}
