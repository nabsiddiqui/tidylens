% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vlm.R
\name{vlm_scale}
\alias{vlm_scale}
\title{Classify shot scale using VLM}
\usage{
vlm_scale(
  tl_images,
  model = "qwen2.5vl",
  base_url = "http://localhost:11434",
  downsample = 512
)
}
\arguments{
\item{tl_images}{A tl_images tibble.}

\item{model}{Model name. Default \code{"qwen2.5vl"} (recommended).}

\item{base_url}{Base URL for Ollama. Default \code{"http://localhost:11434"}.}

\item{downsample}{Max image dimension to send. Default 512.}
}
\value{
The input tibble with added columns:
\itemize{
\item \code{shot_scale}: Close, Medium, or Long.
\item \code{shot_scale_confidence}: 1.0 if the VLM's response matched a known
category, otherwise 0.0.
}
}
\description{
Assign each image a cinematographic shot scale label (Close, Medium, or
Long) using a vision-language model via Ollama. This is the optional
high-accuracy path; the default \code{\link[=film_classify_scale]{film_classify_scale()}} uses a classical
Random Forest on engineered features and requires no VLM.
}
\details{
\subsection{How it works (ELI5)}{

Show the frame to a VLM and ask it whether the shot is close, medium, or
long. The VLM uses its understanding of framing, figure-ground relations,
and cinematographic conventions to pick the best-fitting category.
}

\subsection{Shot Scale Categories}{\tabular{ll}{
   Group \tab What's in Frame \cr
   Close \tab Face or detail fills the frame (ECU, CU, MCU) \cr
   Medium \tab Waist to knees (MS, MLS) \cr
   Long \tab Full body or landscape (LS, ELS) \cr
}


Requires Ollama to be installed and running with a vision-capable model.
The classical Random Forest in \code{\link[=film_classify_scale]{film_classify_scale()}} is the default and
runs without any VLM dependency; use this function when you want the
higher accuracy of a VLM and have Ollama set up.
}
}
\seealso{
\code{\link[=film_classify_scale]{film_classify_scale()}} for the classical default path.

Other vlm: 
\code{\link{vlm_check_dependencies}()},
\code{\link{vlm_check_ollama}()},
\code{\link{vlm_classify}()},
\code{\link{vlm_describe}()},
\code{\link{vlm_list_models}()},
\code{\link{vlm_pull_model}()},
\code{\link{vlm_recognize}()},
\code{\link{vlm_sentiment}()},
\code{\link{vlm_setup_instructions}()}
}
\concept{vlm}
