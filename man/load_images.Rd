% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_images.R
\name{load_images}
\alias{load_images}
\title{Load images into a tl_images tibble}
\usage{
load_images(
  path,
  pattern = "\\\\.(jpg|jpeg|png|gif|bmp|tiff|webp)$",
  recursive = FALSE,
  preserve_metadata = TRUE
)
}
\arguments{
\item{path}{Path to a directory containing images, a single image file,
or a manifest file (CSV/TSV with a \code{source} column).}

\item{pattern}{File pattern to match when scanning directories.
Default matches common image formats.}

\item{recursive}{Scan directories recursively? Default \code{FALSE}.}

\item{preserve_metadata}{When loading from a manifest CSV, preserve
additional columns (e.g., video_source, custom annotations). Default \code{TRUE}.}
}
\value{
A tibble of class \code{tl_images} with columns:
\itemize{
\item \code{id}: Filename without extension.
\item \code{source}: Original path.
\item \code{local_path}: Full local path.
\item \code{width}, \code{height}: Image dimensions.
\item \code{format}: File format.
\item Plus any additional columns from manifest CSV (if preserve_metadata = TRUE).
}
}
\description{
Create a tidy tibble containing image metadata from a directory, file paths,
or manifest file. When loading from a manifest CSV, any additional columns
in the CSV are preserved in the resulting tl_images tibble.
}
\examples{
\dontrun{
# Load from directory
images <- load_images("images/")

# Load specific files
images <- load_images(c("img1.jpg", "img2.png"))

# Load from manifest with extra metadata preserved
# manifest.csv has: source, video_source, scene_name, etc.
images <- load_images("manifest.csv")
# Result includes video_source and scene_name columns
}
}
\seealso{
Other io: 
\code{\link{is_tl_images}()},
\code{\link{print.tl_images}()}
}
\concept{io}
