% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/video.R
\name{frame_extract_shots}
\alias{frame_extract_shots}
\title{Extract shots from video with timing}
\usage{
frame_extract_shots(
  video_path,
  fps = 2,
  method = c("adaptive", "content", "histogram"),
  threshold = 0.5,
  adaptive_threshold = 3,
  window = 2,
  min_content_val = 15,
  min_scene_len = NULL,
  output_dir = tempdir(),
  position = "middle",
  include_style = TRUE
)
}
\arguments{
\item{video_path}{Path to video file.}

\item{fps}{Frames per second to analyze. Higher = more accurate but slower. Default 2.}

\item{method}{Shot-detection method: \code{"adaptive"} (default), \code{"content"}, or
\code{"histogram"} (pre-1.x behaviour). See \code{\link[=detect_shot_changes]{detect_shot_changes()}}.}

\item{threshold}{Shot detection threshold (histogram & content methods). Higher = fewer cuts.}

\item{adaptive_threshold}{Adaptive ratio cutoff (adaptive method). Default 3.0.}

\item{window}{Rolling-mean half-width (adaptive method). Default 2.}

\item{min_content_val}{Minimum absolute HSV score to cut (adaptive method). Default 15.}

\item{min_scene_len}{Minimum gap between cuts, in analysis frames. By default the gap
is derived from \code{fps} (about 0.25 s) to suppress flash/strobe; set to 0 to disable.}

\item{output_dir}{Directory for temporary frames. Default \code{tempdir()}.}

\item{position}{Which frame to keep for each shot: \code{"first"}, \code{"middle"},
or \code{"last"}. Default \code{"middle"}.}

\item{include_style}{Whether to classify shot scale (ECU, CU, etc.). Default \code{TRUE}.}
}
\value{
A tibble with one row per shot containing:
\itemize{
\item \code{shot_id}: Sequential shot number.
\item \code{start_time}: Start time in seconds.
\item \code{end_time}: End time in seconds.
\item \code{duration}: Shot duration in seconds.
\item \code{start_frame}: Frame index of start.
\item \code{end_frame}: Frame index of end.
\item \code{n_frames}: Number of frames in shot.
\item \code{shot_scale}: Broad shot scale group: Close, Medium, or Long
(if include_style = TRUE).
\item \code{shot_scale_confidence}: Random Forest class probability for the predicted shot scale.
\item \code{frame_path}: Path to representative frame image.
}
}
\description{
Analyze a video file directly to detect shots and return timing information.
This is a convenience function that combines frame extraction, shot detection,
and timing calculation in one step.
}
\details{
\subsection{How it works (ELI5)}{

This function watches the video and notices when the scene changes (like when
a movie cuts from one camera angle to another). It tells you when each shot
starts and ends in seconds, plus how long it lasts.

The shot detection algorithm is forwarded to \code{\link[=detect_shot_changes]{detect_shot_changes()}}; see
that function for the full method catalogue. The default \code{"adaptive"} method
self-calibrates to local pacing and is more robust to fast motion and
lighting changes than the original fixed-threshold histogram detector.
}
}
\references{
Chi-squared histogram comparison is a standard technique in video analysis.
See: Lienhart, R. (2001). Reliable Transition Detection in Videos.
\url{https://doi.org/10.1145/500141.500149}
}
\seealso{
Other video: 
\code{\link{detect_shot_changes}()},
\code{\link{frame_extract_by_seconds}()},
\code{\link{frame_extract_evenly}()},
\code{\link{frame_extract_keyframes}()},
\code{\link{video_download}()},
\code{\link{video_get_info}()}
}
\concept{video}
