% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/video.R
\name{video_extract_frames}
\alias{video_extract_frames}
\title{Extract frames from a video}
\usage{
video_extract_frames(
  video_path,
  output_dir = tempdir(),
  fps = NULL,
  frames = NULL,
  format = "jpg",
  prefix = "frame"
)
}
\arguments{
\item{video_path}{Path to video file (single file or vector of paths).}

\item{output_dir}{Directory to save extracted frames. Default is \code{tempdir()}.}

\item{fps}{Frames per second to extract. Mutually exclusive with \code{frames}.}

\item{frames}{Specific frame numbers to extract. Mutually exclusive with \code{fps}.}

\item{format}{Output image format. Default \code{"jpg"}.}

\item{prefix}{Filename prefix for extracted frames. Default \code{"frame"}.}
}
\value{
A tl_images tibble of extracted frames with additional column:
\itemize{
\item \code{video_source}: Path to the original video file.
}
}
\description{
Extract frames from a video file at specified intervals or frame numbers.
Returns a tl_images tibble with video_source column tracking the original video.
}
\examples{
\dontrun{
# Extract 1 frame per second from one video
frames <- video_extract_frames("video.mp4", fps = 1)

# Extract from multiple videos
frames <- video_extract_frames(c("video1.mp4", "video2.mp4"), fps = 1)
}
}
\seealso{
Other video: 
\code{\link{detect_shot_changes}()},
\code{\link{video_download}()},
\code{\link{video_extract_shots}()},
\code{\link{video_get_info}()},
\code{\link{video_sample_frames}()}
}
\concept{video}
